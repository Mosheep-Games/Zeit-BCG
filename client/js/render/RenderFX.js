// RenderFX - minimal visual helpers (placeholders)
window.RenderFX = {
  floatText(text, pos, color) {
    // placeholder: log to console
    console.log('FX', text, pos, color);
  },
  createTrail(a,b,type) { /* placeholder */ },
  createCircle(pos,rad,type){ /* placeholder */ },
  screenshake(v) { /* placeholder */ },
  globalTint(c,a,t) { /* placeholder */ }
};